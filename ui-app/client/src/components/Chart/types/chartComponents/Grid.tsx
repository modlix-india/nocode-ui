import React from 'react';
import { ChartData, ChartProperties } from '../common';

export default function Grid({}: Readonly<{
	properties: ChartProperties;
	chartData: ChartData;
}>) {
	return <></>;
}
