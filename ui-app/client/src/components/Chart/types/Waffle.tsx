import React from 'react';
import { ChartProperties } from './common';

export default function Waffle({
	properties,
	containerRef,
}: {
	properties: ChartProperties;
	containerRef: HTMLDivElement | null;
}) {
	return <></>;
}
