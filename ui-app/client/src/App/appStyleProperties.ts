import { StylePropertyDefinition } from '../types/style';

const appStyleProperties: Array<StylePropertyDefinition> = [
	{
		name: '--main-font-family',
		displayName: 'Application Font Family',
		groupName: 'mainGroup',
		description: 'This font is used everywhere ',
	},
];

export default appStyleProperties;
